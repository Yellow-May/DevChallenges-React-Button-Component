import styled from 'styled-components';

const Row = styled.div({
    display: 'flex',
    alignItems: "center",
    columnGap: 60,
    rowGap: 20,
    flexWrap: 'wrap',
    marginBottom: 60,
})

export default Row;