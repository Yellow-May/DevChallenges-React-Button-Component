import styled from "styled-components";

const Typography = styled.h1({
        fontSize: 24,
        lineHeight: '36px',
        fontWeight: 500,
        color: '#4F4F4F',
        marginBottom: 20,
    })

export default Typography